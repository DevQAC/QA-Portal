import { ControlHostDirective } from './control-host.directive';

describe('ControlHostDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new ControlHostDirective();
  //   expect(directive).toBeTruthy();
  // });
});
