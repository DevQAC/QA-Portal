apiVersion: apps/v1
kind: Deployment
metadata:      
  name: feedback-api
  labels: 
    app: feedback-api
spec:
  selector:
    matchLabels:
      app: feedback-api
  template:
    metadata:
      labels:
        app: feedback-api
    spec:
      containers:
      - name: feedback-api
        image: bobcrutchley/feedback-api:latest
        ports:
        - containerPort: 8080
        env:
          - name: POSTGRES_HOST
            value: postgres-db
---
apiVersion: v1
kind: Service
metadata:
  name: feedback-api
spec:
  selector:
    app: feedback-api
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  type: NodePort
